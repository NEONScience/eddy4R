def.plot.flux.animate <- function(
  input_folder,
  output_file,
  nodata_value = 0,
  colormap = 'YlOrRd',
  basemap_style = 'osm',
  palette_style = 'equal',
  alpha = 0.4,
  legend = TRUE
  ) {
  
  # Load necessary libraries
  library(raster)
  library(tmap)
  library(tmaptools)
  library(gifski)
  
  
  
}